+++
title = "Getting Started"
+++

Install `solradm` and launch the CLI:

```
pip install solradm
solradm --help
```

The CLI uses [AsyncTyper](https://typer.tiangolo.com/) and prints rich-formatted logs for an improved terminal experience. Each command interacts with a Solr cluster using an asynchronous HTTP API.
